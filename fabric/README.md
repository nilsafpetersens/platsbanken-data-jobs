# Fabric items

   Notebooks exported from the Microsoft Fabric workspace `platsbanken-data-jobs`.
   Git sync is disabled in the school's Fabric tenant, so notebooks are exported
   manually after significant changes
